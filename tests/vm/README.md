# Reconstructed fixtures

`test00.asm` through `test20.asm` are exact copies of the raw assembly literals in the corresponding public `TEST_CASE("vmNN")` blocks in `test_parser.cpp`.

The original repository referenced these paths but did not include them. These are reconstructed public test inputs, not a claim that private instructor fixtures were recovered. Verify with `python3 scripts/recover_fixtures.py --check`.
